// performing a task
function greet(name, address)
{
    console.log('Hello ' + name + ' Do you live in ' + address);
}
// performing calculation

function calc(num)
{
   return num * num;
}

console.log(calc(2));